import { describe, expect, it } from 'vitest';

// Prueba de humo: confirma que el arnés de pruebas corre.
// Se reemplaza por las pruebas reales del motor de cupos en la épica E.
describe('arnés de pruebas', () => {
  it('corre', () => {
    expect(true).toBe(true);
  });
});
